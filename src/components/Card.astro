---
interface Props {
	title: string;
	body: string;
	href: string;
}

const { href, title, body } = Astro.props;
---

<li class="border border-2 border-black m-8 p-8 rounded-lg">
	<a href={href}>
		<h2 class="text-2xl">
			{title}
			<span>&rarr;</span>
		</h2>
		<p>
			{body}
		</p>
	</a>
</li>
